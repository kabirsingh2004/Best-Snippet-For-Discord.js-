{
	"!djs": {
		"prefix": "!djs",
		"body": [
			"const { Client, Message, MessageEmbed } = require('discord.js');\r",
			"const ee = require('../../config/bot.json');\r",
			"const config = require('../../config/config.json')\r",
			"\r",
			"module.exports = {\r",
			"    name: '',\r",
            "    aliases: [''], ",
            "    description: '',\r",
			"    usage: '',\r",

			"    /** \r",
			"     * @param {Client} client \r",
			"     * @param {Message} message \r",
			"     * @param {String[]} args \r",
			"     */\r",
			"    run: async(client, message, args) => {\r",
			"\r",
			"    }\r",
			"}"
		],
		"description": "[djs]"
	}
}
